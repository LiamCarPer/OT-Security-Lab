route:
  receiver: ot-webhook
  group_by: [alertname, severity]
  group_wait: 10s
  group_interval: 1m
  repeat_interval: 6h

receivers:
  - name: ot-webhook
    webhook_configs:
      - url: http://ot_webhook:9095/alert
        send_resolved: true
